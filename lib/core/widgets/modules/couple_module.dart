import 'package:flutter/material.dart';

class CoupleModule extends StatelessWidget {
  const CoupleModule({super.key});

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: () {

        // navegar
        // lógica
        // firebase
      },

      child: Container(
        height: 80,
        color: Colors.green,
      ),
    );
  }
}